{
  "name": "ayntravel",
  "version": "1.0.0",
  "private": true,
  "description": "AYN TRAVEL — ayntravel.uz. Backend va frontend mustaqil ilovalar.",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "sync:shared": "node sync-shared.mjs",
    "db:export": "cd backend && npm run db:export",
    "typecheck": "cd backend && npm run typecheck && cd ../frontend && npm run typecheck"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
